---
native:
  slug: alias-from-pattern
aliases:
- from: 'pattern:sds-hyperledger-*-jira'  # when you specify 'pattern:' prefix everything after that prefix will not be changed
  to:                                     # 'sds-' prefix won't be added, '/' won't be changed to '-' and so on.
  - hyperledger/jira/all
